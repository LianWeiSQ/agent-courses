# Test Framework Guide

## 阅读目标

- 先理解 `用户记忆评估框架` 在课程中的角色。
- 再对照同目录下的 `src/main.rs`，看概念如何被类型和函数表达。
- 最后再决定是否接入真实 API、数据集或训练环境。

## 实现关注点

- 把隐式脚本流程改成显式状态和函数。
- 把容易散落在 prompt 或配置里的规则写进可测试代码。
- 保持原目录位置，方便和课程章节一一对应。

## 建议练习

1. 运行当前目录对应的 `cargo run -p ...`。
2. 修改一个参数或状态转移。
3. 添加一个测试，证明行为符合预期。
