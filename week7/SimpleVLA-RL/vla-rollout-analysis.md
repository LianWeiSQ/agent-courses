# Vla Rollout Analysis

这份文章已按 Rust 重构版重新整理。

## 阅读目标

- 先理解 `视觉-语言-动作 RL` 在课程中的角色。
- 再对照同目录下的 `src/main.rs`，看概念如何被 Rust 类型表达。
- 最后再决定是否接入真实 API、数据集或训练环境。

## Rust 版关注点

- 把隐式脚本流程改成显式状态和函数。
- 把容易散落在 prompt 或配置里的规则写进可测试代码。
- 保持原目录位置，方便和课程章节一一对应。

## 建议练习

1. 运行当前目录对应的 `cargo run -p ...`。
2. 修改一个参数或状态转移。
3. 添加一个测试，证明行为符合预期。
